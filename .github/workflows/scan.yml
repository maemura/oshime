name: かぶのすけ 自動スキャン

on:
  schedule:
    # 毎朝 7:30 JST = UTC 22:30（前日）月〜金
    - cron: '30 22 * * 0-4'
    # 大引け後 16:00 JST = UTC 7:00 月〜金
    - cron: '0 7 * * 1-5'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    permissions:
      contents: write

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python 3.11 セットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 依存パッケージをインストール
        run: |
          pip install yfinance pandas numpy requests xlrd openpyxl

      - name: fetch_stocks.py を実行
        run: python3 fetch_stocks.py
        env:
          TZ: Asia/Tokyo

      - name: stocks_data.json をコミット & プッシュ
        run: |
          git config user.name  "かぶのすけBot"
          git config user.email "bot@kabunosuke.app"
          git add stocks_data.json
          if git diff --cached --quiet; then
            echo "データに変更なし。スキップ。"
          else
            git commit -m "📊 自動更新 $(TZ=Asia/Tokyo date '+%Y/%m/%d %H:%M JST')"
            git push
          fi
